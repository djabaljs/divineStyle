{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link href="{{asset('concept/assets/vendor/fonts/circular-std/style.css')}}" rel="stylesheet">
    <link rel="stylesheet" href="{{asset('concept/assets/libs/css/style.css')}}">
    <link rel="stylesheet" href="{{asset('concept/assets/vendor/fonts/fontawesome/css/fontawesome-all.css')}}">
    <link rel="stylesheet" type="text/css" href="{{asset('concept/assets/vendor/datatables/css/dataTables.bootstrap4.css')}}">
    <link rel="stylesheet" type="text/css" href="{{asset('concept/assets/vendor/datatables/css/buttons.bootstrap4.css')}}">
    <link rel="stylesheet" type="text/css" href="{{asset('concept/assets/vendor/datatables/css/select.bootstrap4.css')}}">
    <link rel="stylesheet" type="text/css" href="{{asset('concept/assets/vendor/datatables/css/fixedHeader.bootstrap4.css')}}">
{% endblock %}
{% block body %}
<div class="container-fluid dashboard-content ">
      <!-- ============================================================== -->
                <!-- pageheader -->
                <!-- ============================================================== -->
                <div class="row">
                    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                        <div class="page-header">
                            <h2 class="pageheader-title">Data Tables</h2>
                            <p class="pageheader-text">Proin placerat ante duiullam scelerisque a velit ac porta, fusce sit amet vestibulum mi. Morbi lobortis pulvinar quam.</p>
                            <div class="page-breadcrumb">
                                <nav aria-label="breadcrumb">
                                    <ol class="breadcrumb">
                                        <li class="breadcrumb-item"><a href="#" class="breadcrumb-link">Dashboard</a></li>
                                        <li class="breadcrumb-item"><a href="#" class="breadcrumb-link">Tables</a></li>
                                        <li class="breadcrumb-item active" aria-current="page">Data Tables</li>
                                    </ol>
                                </nav>
                            </div>
                        </div>
                    </div>
                </div>
                 <div class="row">
                    <div class="col-xl-6 col-lg-6 col-md-6 col-sm-12 col-12">
                     <h5>Gestionnaire: {{shop.manager}}</h5>
                     <h5>Téléphone: {{shop.manager.phone}}</h5>
                    </div>
                 </div>
                 <br />
                <div class="row">
                 <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 col-12">
                                <div class="card border-3 border-top border-top-primary">
                                    <div class="card-body">
                                        <h5 class="text-muted">Ventes</h5>
                                        <div class="metric-value d-inline-block">
                                            <h1 class="mb-1">{{totalPaid|format_currency('XOF')}}</h1>
                                        </div>
                                        <div class="metric-label d-inline-block float-right text-success font-weight-bold">
                                            {# <span class="icon-circle-small icon-box-xs text-success bg-success-light"><i class="fa fa-fw fa-arrow-up"></i></span><span class="ml-1">10%</span> #}
                                        </div>
                                    </div>
                                </div>
                    </div>
                         <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 col-12">
                                <div class="card border-3 border-top border-top-primary">
                                    <div class="card-body">
                                        <h5 class="text-muted">Clients</h5>
                                        <div class="metric-value d-inline-block">
                                            <h1 class="mb-1">{{shop.customers|length}}</h1>
                                        </div>
                                        <div class="metric-label d-inline-block float-right text-success font-weight-bold">
                                            {# <span class="icon-circle-small icon-box-xs text-success bg-success-light"><i class="fa fa-fw fa-arrow-up"></i></span><span class="ml-1">10%</span> #}
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 col-12">
                                <div class="card border-3 border-top border-top-primary">
                                    <div class="card-body">
                                        <h5 class="text-muted">Produits</h5>
                                        <div class="metric-value d-inline-block">
                                            <h1 class="mb-1">{{shop.products|length}}</h1>
                                        </div>
                                        <div class="metric-label d-inline-block float-right text-success font-weight-bold">
                                            {# <span class="icon-circle-small icon-box-xs text-success bg-success-light"><i class="fa fa-fw fa-arrow-up"></i></span><span class="ml-1">10%</span> #}
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 col-12">
                                <div class="card border-3 border-top border-top-primary">
                                    <div class="card-body">
                                        <h5 class="text-muted">Montant dû</h5>
                                        <div class="metric-value d-inline-block">
                                            <h1 class="mb-1">{{totalAmount|format_currency('XOF')}}</h1>
                                        </div>
                                        <div class="metric-label d-inline-block float-right text-success font-weight-bold">
                                            {# <span class="icon-circle-small icon-box-xs text-success bg-success-light"><i class="fa fa-fw fa-arrow-up"></i></span><span class="ml-1">10%</span> #}
                                        </div>
                                    </div>
                                </div>
                            </div>
                             <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 col-12">
                                <div class="card border-3 border-top border-top-primary">
                                    <div class="card-body">
                                        <h5 class="text-muted">Total commande</h5>
                                        <div class="metric-value d-inline-block">
                                            <h1 class="mb-1">{{shop.orders|length}}</h1>
                                        </div>
                                        <div class="metric-label d-inline-block float-right text-success font-weight-bold">
                                            {# <span class="icon-circle-small icon-box-xs text-success bg-success-light"><i class="fa fa-fw fa-arrow-up"></i></span><span class="ml-1">10%</span> #}
                                        </div>
                                    </div>
                                </div>
                            </div>
                              <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 col-12">
                                <div class="card border-3 border-top border-top-primary">
                                    <div class="card-body">
                                        <h5 class="text-muted">Livraison effectuée</h5>
                                        <div class="metric-value d-inline-block">
                                            <h1 class="mb-1">{{deliveryIsSuccessfully|length}}/{{deliveryIsNotSuccessfully|length}}</h1>
                                        </div>
                                        <div class="metric-label d-inline-block float-right text-success font-weight-bold">
                                            {# <span class="icon-circle-small icon-box-xs text-success bg-success-light"><i class="fa fa-fw fa-arrow-up"></i></span><span class="ml-1">10%</span> #}
                                        </div>
                                    </div>
                                </div>
                            </div>
                    </div>
                    
                <br/>
     
                <!-- ============================================================== -->
                <!-- end pageheader -->
                <!-- ============================================================== -->
                   <div class="row">
                    <!-- ============================================================== -->
                    <!-- data table  -->
                    <!-- ============================================================== -->
                    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Liste des produits</h5>
                            </div>
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table id="example" class="table table-striped table-bordered second" style="width:100%">
                                        <thead>
                                            <tr>
                                                <th>N°</th>
                                                <th>Images</th>
                                                <th>Noms</th>
                                                <th>Catégories</th>
                                                <th>Prix d'achat</th>
                                                <th>Prix  de vente</th>
                                                <th>Qt en stock</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                           {% for product in shop.products %}
                                             <tr>
                                                <td>{{loop.index}}</td>
                                                <td>
                                                {% if product.imageUrls is not empty  %}
                                                <img src="{{product.imageUrls[0]}}" class="img-thumbnail" style="width:100px" />
                                                {% else %}
                                                <p>Aucune image</p>
                                                {% endif %}
                                                </td>
                                                <td>{{product.name}}</td>
                                                <td>
                                                {{product.category.name}}
                                                </td>
                                                <td>{{product.buyingPrice}}</td>
                                                <td>{{product.sellingPrice}}</td>
                                                <td {% if product.minimumStock == product.quantity or product.quantity == 0  %} style="background:red;color:#FFF"{% else %} style="background:green; color:#FFF"{% endif %}>
                                                 {{ product.quantity }}
                                                </td>
                                                <td>
                                                <a href="{{path('admin_products_show', {slug: product.slug})}}" class="btn btn-info btn-xs"><i class="fas fa-eye"></i></a>
                                                <a href="{{path('admin_products_update', {slug: product.slug})}}" class="btn btn-warning btn-xs"><i class="fas fa-edit"></i></a>
                                                <a href="{{path('admin_products_delete', {slug: product.slug})}}" class="btn btn-danger btn-xs"><i class="fas fa-trash"></i></a>
                                                </td>
                                            </tr>
                                           {% endfor %}
                                        </tbody>
                                        <tfoot>
                                            <tr>
                                                <th>N°</th>
                                                <th>Images</th>
                                                <th>Noms</th>
                                                <th>Catégories</th>
                                                <th>Prix d'achat</th>
                                                <th>Prix  de vente</th>
                                                <th>Qt en stock</th>
                                                <th>Actions</th>
                                            </tr>
                                        </tfoot>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- ============================================================== -->
                    <!-- end data table  -->
                    <!-- ============================================================== -->
                </div>
                    <div class="row">
                    <!-- ============================================================== -->
                    <!-- data table  -->
                    <!-- ============================================================== -->
                    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Liste des ventes effectuées</h5>
                            </div>
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table id="example" class="table table-striped table-bordered second" style="width:100%">
                                        <thead>
                                            <tr>
                                                <th>N°</th>
                                                <th>N° commande</th>
                                                <th>Client</th>
                                                <th>Montant Payer</th>
                                                <th>Reste à Payer</th>
                                                <th>Date</th>
                                                <th>Heure</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                         {% for payment in payments %}
                                            <tr>
                                                <td>{{loop.index}}</td>
                                                <td>{{payment.invoice.orders.orderNumber}}</td>
                                                <td>{{payment.invoice.orders.customer}}</td>
                                                <td>{{payment.invoice.orders.saleTotal| format_currency('XOF') }}</td>
                                                <td>{{payment.amount| format_currency('XOF') }}</td>
                                                <td>{{payment.invoice.orders.createdAt|date('Y-m-d')}}</td>
                                                <td>{{payment.invoice.orders.createdAt|date('H:m')|format_datetime(pattern="H 'h' m ") }}</td>
                                                 <td>
                                                    <a href="{{path('admin_products_orders_show', {id: payment.invoice.orders.id})}}" class="btn btn-info"><i class="fas fa-eye"></i></a>
                                                    {# <a href="{{path('admin_products_orders_update', {id: order.id})}}" class="btn btn-warning"><i class="fas fa-edit"></i></a> #}
                                                    <a href="{{path('admin_products_orders_invoice_show', {id: payment.invoice.id})}}" class="btn btn-primary"><i class="fas fa-file"></i></a>
                                                    <a href="{{path('admin_products_orders_delete', {id: payment.invoice.orders.id})}}" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                                                </td>
                                            </tr>
                                         {% endfor %}
                                        </tbody>
                                        <tfoot>
                                              <tr>
                                                <th>N°</th>
                                                <th>N° commande</th>
                                                <th>Client</th>
                                                <th>Montant Payer</th>
                                                <th>Reste à Payer</th>
                                                <th>Date</th>
                                                <th>Heure</th>
                                                <th>Actions</th>
                                            </tr>
                                        </tfoot>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- ============================================================== -->
                    <!-- end data table  -->
                    <!-- ============================================================== -->
                </div>
                      <div class="row">
                    <!-- ============================================================== -->
                    <!-- data table  -->
                    <!-- ============================================================== -->
                    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Liste des commandes effectuées</h5>
                            </div>
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table id="example" class="table table-striped table-bordered second" style="width:100%">
                                        <thead>
                                            <tr>
                                                <th>N°</th>
                                                <th>N° commande</th>
                                                <th>Client</th>
                                                <th>Montant</th>
                                                <th>Date</th>
                                                <th>Heure</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                         {% for payment in payments %}
                                            <tr>
                                                <td>{{loop.index}}</td>
                                                <td>{{payment.invoice.orders.orderNumber}}</td>
                                                <td>{{payment.invoice.orders.customer}}</td>
                                                <td>{{payment.amount| format_currency('XOF') }}</td>
                                                <td>{{payment.invoice.orders.createdAt|date('Y-m-d')}}</td>
                                                <td>{{payment.invoice.orders.createdAt|date('H:m')|format_datetime(pattern="H 'h' m ") }}</td>
                                                 <td>
                                                    <a href="{{path('admin_products_orders_show', {id: payment.invoice.orders.id})}}" class="btn btn-info"><i class="fas fa-eye"></i></a>
                                                    {# <a href="{{path('admin_products_orders_update', {id: order.id})}}" class="btn btn-warning"><i class="fas fa-edit"></i></a> #}
                                                    <a href="{{path('admin_products_orders_delete', {id: payment.invoice.orders.id})}}" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                                                </td>
                                            </tr>
                                         {% endfor %}
                                        </tbody>
                                        <tfoot>
                                              <tr>
                                                <th>N°</th>
                                                <th>N° commande</th>
                                                <th>Client</th>
                                                <th>Montant</th>
                                                <th>Date</th>
                                                <th>Heure</th>
                                                <th>Actions</th>
                                            </tr>
                                        </tfoot>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- ============================================================== -->
                    <!-- end data table  -->
                    <!-- ============================================================== -->
                </div>
                  <div class="row">
                    <!-- ============================================================== -->
                    <!-- data table  -->
                    <!-- ============================================================== -->
                    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Liste des livraisons</h5>
                            </div>
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table id="example" class="table table-striped table-bordered second" style="width:100%">
                                        <thead>
                                            <tr>
                                                <th>N°</th>
                                                <th>Livreur</th>
                                                <th>N°Commande</th>
                                                <th>Récepteur</th>
                                                <th>Adresse</th>
                                                <th>Téléphone</th>
                                                <th>Montant de livraison</th>
                                                <th>Statut</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                           {% for delivery in deliveries %}
                                             <tr>
                                                <td>{{loop.index}}</td>
                                                <td>{{delivery.deliveryMan}}</td>
                                                <td>{{delivery.order.orderNumber}}</td>
                                                <td>{{delivery.recipient}}</td>
                                                <td>{{delivery.address}}</td>
                                                <td>{{delivery.recipientPhone}}</td>
                                                <td>{{delivery.amountPaid|format_currency('XOF') }}</td>
                                                <td>{{delivery.status == null? 'Pas livré': 'Livré' }}</td>
                                                <td>
                                                    {# <a href="{{path('admin_category_show', {id: category.id})}}" class="btn btn-info"><i class="fas fa-eye"></i></a> #}
                                                    <a href="{{path('admin_orders_deliveries_update', {id: delivery.id})}}" class="btn btn-warning"><i class="fas fa-edit"></i></a>
                                                    <a href="{{path('admin_orders_deliveries_delete', {id: delivery.id})}}" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                                                </td>
                                            </tr>
                                           {% endfor %}
                                        </tbody>
                                        <tfoot>
                                          <tr>
                                                <th>N°</th>
                                                <th>Livreur</th>
                                                <th>N°Commande</th>
                                                <th>Récepteur</th>
                                                <th>Adresse</th>
                                                <th>Téléphone</th>
                                                <th>Montant de livraison</th>
                                                <th>Statut</th>
                                                <th>Actions</th>
                                            </tr>
                                        </tfoot>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- ============================================================== -->
                    <!-- end data table  -->
                    <!-- ============================================================== -->
                </div>
                     
</div>
{% endblock %}
{% block javascripts %}
    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script src="https://cdn.datatables.net/1.10.22/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.10.22/js/dataTables.bootstrap4.min.js"></script>


    <script>
        $(document).ready(function() {
    $('#example').DataTable({
         buttons: [
        'pdf'
    ]
    });
   
    } );
    </script>
{% endblock %}